spring:
  datasource:
    url: jdbc:mysql://localhost:3306/levameulivro_db?allowPublicKeyRetrieval=true&useSSL=false
    username: JORGE
    password: JORGE
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
